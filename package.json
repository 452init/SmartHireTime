{
  "name": "smart-hire-time",
  "version": "1.0.0",
  "description": "A simple Python and TypeScript interview question generator.",
  "type": "module",
  "scripts": {
    "build": "vite build",
    "dev": "vite --host 127.0.0.1",
    "start": "PYTHON_BIN=$( [ -x .venv/bin/python ] && echo .venv/bin/python || echo python ) && $PYTHON_BIN backend/app.py",
    "check": "tsc --noEmit && PYTHON_BIN=$( [ -x .venv/bin/python ] && echo .venv/bin/python || echo python ) && $PYTHON_BIN -m py_compile backend/app.py backend/config.py backend/ai_api.py backend/database.py backend/question_builder.py"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vite": "^6.0.7"
  },
  "engines": {
    "node": ">=18",
    "python": ">=3.10"
  }
}
